#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webpack-deploy/bin/deploy-redis" "$@"
  ret=$?
else 
  node  "$basedir/../webpack-deploy/bin/deploy-redis" "$@"
  ret=$?
fi
exit $ret
